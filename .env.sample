TIDB_DSN="mysql+pymysql://user:password@host:4000/dbname"
PORT=8000
AUTH_ENABLED=1
SESSION_SECRET="changeme-super-secret"
APP_BASE_URL="http://localhost:8000"
EMAIL_ENABLED=0
DEMO_MODE=1
ROUTEFORGE_PRIMARY_PROJECT_ID=1

# S3-compatible artifact storage
S3_COMPAT_ENDPOINT="https://account-id.r2.cloudflarestorage.com"
S3_BUCKET="routeforge-artifacts"
S3_ACCESS_KEY=""
S3_SECRET_KEY=""
# Optional overrides:
# S3_REGION="auto"
# S3_PUBLIC_BASE_URL="https://cdn.example.com"
# S3_PRESIGN_EXPIRES=900

# Attestation minting configuration
MINT_MODE=demo  # demo|testnet|off
BASE_RPC_URL="https://sepolia.base.org"
# Provide custodial signer private key when minting without a wallet; leave empty to require client wallet.
MINT_PRIVKEY=""
NFT_CONTRACT=""
NFT_ABI_PATH="contracts/erc721.abi.json"
NFT_MINT_FN="safeMint(address,string)"  # or mint(address,string)

# --- Testnet mint (optional) ---
# MINT_MODE=testnet
# BASE_RPC_URL=
# NFT_CONTRACT=
# MINT_PRIVKEY=

# --- Wallet mint (ERC-721) ---
# NFT_CONTRACT=0xYourSepoliaERC721
# NFT_ABI_PATH=contracts/erc721.abi.json
# NFT_MINT_FN=safeMint(address,string)   # if your contract differs, update this
